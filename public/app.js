const savedHorizons=(()=>{try{return JSON.parse(localStorage.getItem('ffPairHorizons')||'{}')}catch{return{}}})();
const state={pair:'EURUSD',horizon:Number(savedHorizons.EURUSD)||1,pairHorizons:savedHorizons,serverOffset:0,poll:null,engine:null,initialized:false,tvKey:'',waitingFirstSignal:false,selectedAt:Date.now(),shownSignalKey:'',boundaryRefreshKey:''};
const $=id=>document.getElementById(id),pair=$('pair'),horizons=$('horizons'),orb=$('signalButton'),direction=$('direction'),prob=$('probability'),status=$('status'),countdown=$('countdown'),body=$('historyBody'),serverNow=()=>Date.now()+state.serverOffset;
const tvSymbols={EURUSD:'FX_IDC:EURUSD',EURJPY:'FX_IDC:EURJPY',GBPUSD:'FX_IDC:GBPUSD',AUDJPY:'FX_IDC:AUDJPY',AUDUSD:'FX_IDC:AUDUSD',USDJPY:'FX_IDC:USDJPY',NZDCHF:'FX_IDC:NZDCHF',USDPKR:'FX_IDC:USDPKR',USDINR:'FX_IDC:USDINR',BTCUSD:'COINBASE:BTCUSD',XAUUSD:'OANDA:XAUUSD'},tvIntervals={1:'1',2:'1',3:'3',5:'5',15:'15'};
function batchMeta(sample=0){const n=Math.max(0,Number(sample)||0),completed=Math.floor(n/20),progress=n%20,current=completed+1;return{sample:n,completed,progress,current,label:`Batch #${current} · ${progress}/20`,completedLabel:completed?`${completed} completed batch${completed===1?'':'es'}`:'No completed batches yet'}}
function savePairHorizon(){state.pairHorizons[state.pair]=state.horizon;localStorage.setItem('ffPairHorizons',JSON.stringify(state.pairHorizons))}
async function syncBackgroundHorizon(p=state.pair,h=state.horizon){try{return await fetch(`/api/research/background/${p}/horizon`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({horizon:Number(h)})}).then(r=>r.json())}catch{return null}}
function markHorizon(){[...horizons.children].forEach(b=>b.classList.toggle('active',Number(b.dataset.m)===state.horizon))}
function tvSelectionKey(){return`${state.pair}:${state.horizon}`}
function renderTradingView(force=false){const host=$('tradingviewChart');if(!host)return;const key=tvSelectionKey();if(!force&&state.tvKey===key)return;state.tvKey=key;const mobile=window.innerWidth<=650,chartHeight=mobile?360:480;host.style.height=`${chartHeight}px`;host.innerHTML='<div class="tradingview-widget-container__widget"></div>';const script=document.createElement('script');script.type='text/javascript';script.src='https://s3.tradingview.com/external-embedding/embed-widget-advanced-chart.js';script.async=true;script.text=JSON.stringify({autosize:false,width:'100%',height:chartHeight,symbol:tvSymbols[state.pair]||`FX_IDC:${state.pair}`,interval:tvIntervals[state.horizon]||'1',timezone:'Etc/UTC',theme:'dark',style:'1',locale:'en',backgroundColor:'rgba(5, 2, 11, 1)',gridColor:'rgba(156, 92, 255, 0.08)',hide_top_toolbar:false,hide_legend:false,allow_symbol_change:false,save_image:false,calendar:false,support_host:'https://www.tradingview.com'});host.appendChild(script);$('tvState').textContent=`${state.pair} · ${state.horizon}M`}
async function syncClock(){try{const t0=Date.now(),r=await fetch('/api/time',{cache:'no-store'}),x=await r.json(),t1=Date.now();if(Number.isFinite(x.serverTime))state.serverOffset=x.serverTime-((t0+t1)/2)}catch{}}
function updateClock(){const d=new Date(serverNow());$('marketClock').textContent=d.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit',second:'2-digit'});updateCountdown()}
async function boot(){await syncClock();const cfg=await fetch('/api/config',{cache:'no-store'}).then(r=>r.json());cfg.pairs.forEach(p=>pair.add(new Option(p,p)));cfg.horizons.forEach(m=>{const b=document.createElement('button');b.type='button';b.textContent=`${m}M`;b.dataset.m=m;b.addEventListener('click',()=>selectHorizon(m));horizons.appendChild(b)});pair.value=state.pair;state.horizon=Number(state.pairHorizons[state.pair])||1;markHorizon();savePairHorizon();await syncBackgroundHorizon();pair.addEventListener('change',async()=>{state.pair=pair.value;state.horizon=Number(state.pairHorizons[state.pair])||1;state.selectedAt=Date.now();state.shownSignalKey='';state.boundaryRefreshKey='';markHorizon();savePairHorizon();state.waitingFirstSignal=true;showFalconAnalyzing();await syncBackgroundHorizon();state.tvKey='';renderTradingView(true);setTimeout(refreshState,150)});orb.onclick=startEngine;$('stopButton').onclick=stopEngine;renderTradingView(true);await refreshState();state.initialized=true;state.poll=setInterval(refreshState,800);setInterval(syncClock,30000);setInterval(updateClock,200)}
async function selectHorizon(m){state.horizon=Number(m)||1;state.selectedAt=Date.now();state.shownSignalKey='';state.boundaryRefreshKey='';markHorizon();savePairHorizon();state.waitingFirstSignal=true;showFalconAnalyzing();await syncBackgroundHorizon();state.tvKey='';renderTradingView(true);setTimeout(refreshState,150)}
function setControlsLocked(){pair.disabled=false;[...horizons.children].forEach(b=>b.disabled=false)}
async function startEngine(){if(state.engine?.running){status.textContent='Analysis is already running';return}state.waitingFirstSignal=true;state.selectedAt=Date.now();state.shownSignalKey='';showFalconAnalyzing();try{await syncBackgroundHorizon();const r=await fetch('/api/engine/start',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({pair:state.pair,horizon:state.horizon})}),x=await r.json();if(!r.ok)throw new Error(x.error||'Unable to start engine');applyState(x)}catch(e){state.waitingFirstSignal=false;status.textContent=e.message}}
async function stopEngine(){try{const r=await fetch('/api/engine/stop',{method:'POST'}),x=await r.json();state.waitingFirstSignal=false;applyState(x)}catch(e){status.textContent=e.message}}
async function refreshState(){try{const r=await fetch('/api/engine/state',{cache:'no-store'}),x=await r.json();if(r.ok)applyState(x)}catch{status.textContent='Reconnecting to server…'}}
function selectedPairResearch(x){const pr=(x.backgroundResearch||[]).find(r=>r.pair===state.pair);if(!pr)return null;return pr.horizons?.[state.horizon]||pr.horizons?.[String(state.horizon)]||(Number(pr.horizon)===Number(state.horizon)?pr:null)}
function selectedPairSignal(x){const pr=selectedPairResearch(x);if(!pr)return null;const s=pr.displaySignal||pr.currentCandidate||null;if(!s||s.pair!==state.pair||Number(s.horizon)!==Number(state.horizon))return null;if(state.waitingFirstSignal&&Number(s.generatedAt||0)<state.selectedAt-100)return null;return s}
function signalKey(s){return s?`${s.pair}:${s.horizon}:${Number(s.signalBoundary||s.boundary||0)}:${Number(s.generatedAt||0)}`:''}
function applyState(x){state.engine=x;if(Number.isFinite(x.serverTime))state.serverOffset=x.serverTime-Date.now();const pr=selectedPairResearch(x),ps=selectedPairSignal(x);if(ps){state.waitingFirstSignal=false;state.shownSignalKey=signalKey(ps)}setControlsLocked();$('stopButton').classList.toggle('hidden',!x.running);renderHistory(pr?.recent||[],pr);renderSignal(x,ps,pr);updateCountdown()}
function showFalconAnalyzing(){orb.className='signal-orb idle analyzing falcon-analyzing';direction.textContent='ANALYSING';prob.textContent='MARKET';status.textContent=`Falcon is analysing ${state.pair} ${state.horizon}M independently…`}
function renderSignal(x,ps,pr){const bm=batchMeta(pr?.sample||0);if(state.waitingFirstSignal&&!ps){showFalconAnalyzing();return}if(ps&&ps.direction){const conf=Number(ps.confidence??0),preview=ps.signalType==='LIVE_TIMEFRAME_ANALYSIS';orb.className=`signal-orb ${String(ps.direction).toLowerCase()}`;direction.textContent=ps.direction;prob.textContent=`${conf.toFixed(1)}%`;if(preview){status.textContent=`${state.pair} · ${state.horizon}M live timeframe analysis · fresh ${ps.analysisTimeframe||`${state.horizon}M`} candles · ${bm.label}`}else{status.textContent=ps.qualified?`${state.pair} · ${state.horizon}M boundary-qualified signal · ${bm.label}`:`${state.pair} · ${state.horizon}M boundary analysis · below 60% · ${bm.label}`}return}if(pr?.running){showFalconAnalyzing();return}orb.className='signal-orb idle';direction.textContent='START';prob.textContent='ANALYSIS';status.textContent=`Background research active · ${state.pair} ${state.horizon}M · ${bm.label}`}
function updateCountdown(){const pr=state.engine?selectedPairResearch(state.engine):null,target=Number(pr?.targetBoundary||0),now=serverNow();if(!target){countdown.textContent=`${String(state.horizon).padStart(2,'0')}:00`;return}const raw=target-now,left=Math.max(0,raw);countdown.textContent=`${String(Math.floor(left/60000)).padStart(2,'0')}:${String(Math.floor((left%60000)/1000)).padStart(2,'0')}`;if(raw<=0&&raw>-1500){const k=`${state.pair}:${state.horizon}:${target}`;if(state.boundaryRefreshKey!==k){state.boundaryRefreshKey=k;refreshState();setTimeout(refreshState,350);setTimeout(refreshState,900)}}}
function renderHistory(rows,pr=null){const pairRows=(rows||[]).filter(s=>s.pair===state.pair&&Number(s.horizon)===Number(state.horizon)&&Number(s.confidence)>=60).slice(0,20),bm=batchMeta(pr?.sample||0);if(!pairRows.length){body.innerHTML=`<tr class="empty"><td colspan="7">No completed ${state.pair} ${state.horizon}M qualified results yet</td></tr>`;$('record').textContent='0 / 0';$('winrate').textContent='--';const ss=$('sessionState');if(ss)ss.textContent=`${state.pair} · ${state.horizon}M · ${bm.label}`;return}body.innerHTML=pairRows.map((s,i)=>`<tr><td>${pairRows.length-i}</td><td>${s.pair}</td><td>${s.analysisTimeframe||`${s.horizon}M`}</td><td class="${s.direction==='BUY'?'buy-text':'sell-text'}">${s.direction}</td><td>${Number(s.confidence??0).toFixed(1)}%</td><td>${new Date(s.resolvedAt||s.signalBoundary).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</td><td class="${(s.result||'PENDING').toLowerCase()}">${s.result||'PENDING'}</td></tr>`).join('');const resolved=pairRows.filter(s=>s.result==='WIN'||s.result==='LOSS'),wins=resolved.filter(s=>s.result==='WIN').length;$('record').textContent=`${wins} / ${resolved.length}`;$('winrate').textContent=resolved.length?`${Math.round(wins/resolved.length*100)}%`:'--';const ss=$('sessionState');if(ss)ss.textContent=`${state.pair} · ${state.horizon}M · ${bm.label}`}
boot();
