Repair branch initialized. Railway inspection confirms the OTC collector and bridge are healthy, while `quotex-otc-audit-1000ms` is the failed component because its expected `/data/quotex-otc` path is unavailable in that service. Next implementation must remove the local-volume assumption or attach the canonical data source, and align auditing with the app's 500 ms simulated execution baseline.